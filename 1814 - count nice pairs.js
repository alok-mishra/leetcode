function countNicePairs(nums) {
    var rev = function (n) { return parseInt(n.toString().split('').reverse().join('')); };
    var countMap = {};
    var nice = 0;
    nums.forEach(function (num) {
        console.log("hi");
    });
    return 2;
}
;
countNicePairs([42, 11, 1, 97]);
countNicePairs([13, 10, 35, 24, 76]);
